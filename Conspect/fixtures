Фикстуры нужны для того чтобы загружать и забирать данные если база данных удалится 

Если есть фикстуры то их легко можно загрузить (базу данных)
Но перед этим в корне нашего приложения надо создать папку fixtures и под папки по имени загружаемой таблицы например если это товары goods если это каталог catalog (можно называть как хотим просто для нас чтоб было понятней)

1. Загрузка фикстур на сервер
manage.py dumpdata [что загружаем таблицу] > [куда fixtures/загружаем/название.json] 
manage.py dumpdata goods.Catalog > fixtures/goods/catalog.json

2. Загружаем фикстуры в базу данных
manage.py loaddata [fixtures/путь до файла.json]
manage.py loaddata fixtures/goods/catalog.json